import { createApp } from 'vue'  //import the createApp function from the vue library
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')  //create the app and mount it to the #app element
